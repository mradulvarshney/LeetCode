class Solution {
    public boolean isPowerOfTwo(int n) 
    {
        long i=1;
        while(i<=n)
        {
            if(i==n)
                return true;
            
            i*=2;
            
            if(i>n)
                break;
        }
        return false;
    }
}
