class Solution {
    public int fib(int n) 
    {
        int a=0, b=1;
        if(n==0)
            return a;
        
        while(n-- > 1)
        {
            int c = a+b;
            a=b;
            b=c;
        }
        return b;
    }
}
